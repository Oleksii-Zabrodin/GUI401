/*
 * Auxiliary.h
 *
 *  Created on: Mar 26, 2025
 *      Author: lex
 */

#ifndef AUXILIARY_H_
#define AUXILIARY_H_

//namespace Auxiliary {
//	template <typename T>
//	T max(T x, T y) { return x < y ? y : x; };
//	template <typename T>
//	T min(T x, T y) { return x < y ? x : y; };
//	template <typename T>
//	T bound(T minValue, T x, T maxValue) { return min(max(minValue, x), maxValue); };


	int maxi(int x, int y);
	int mini(int x, int y);
	int boundi(int minValue, int x, int maxValue);
//}

#endif /* AUXILIARY_H_ */
